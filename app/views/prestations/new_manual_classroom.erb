<div id="form_actions">
	<h3>Saisie / Pointage rapide des présences</h3>
	<b>Le: <%= @date %></b>
	<b>Classe: <%= Classroom.find(@classe).nom %></b>
	<br><br>
	Cochez les prestations effectuées et n'oubliez pas d'enregistrer.<br>
	Notez que décocher une prestation déjà enregistrée ne la supprimera pas automatiquement. Si vous souhaitez vraiment la supprimer, utiliser le bouton 'modifier' en face de chaque jour/enfant dans 
	la <%= link_to "liste des prestations", prestations_path(prestation_date:@date, classe:@classe) %>.  
</div>

<div style="color:green" id="msgzone"></div>

<%= form_for(Prestation.new, :url => { :controller => "prestations", :action => "new_manual_classroom_check"}) do |f| %>
	<table class="sample">
	    <tr>
	      <th>Nom</th>
	      <th>Prénom</th>
	      <th><%= check_box_tag "check_all0", false %>Repas</th>
	      <th><%= check_box_tag "check_all1", false %>Garderie Matin</th>
	      <th><%= check_box_tag "check_all2", false %>Garderie Soir</th>
	      <th><%= check_box_tag "check_all3", false %>Centre Matin</th>
	      <th><%= check_box_tag "check_all4", false %>Centre Soir</th>
    	</tr>

	    <% @kids_to_show.each_with_index do |enfant, index| %>
			<% if @kids_to_show_presta[index] %>
				 <% @presta = @kids_to_show_presta[index] %>
			<% else %>
				 <% @presta = Prestation.new %>
			<% end %>	
			<tr>
			  <td><%= enfant.nomfamille %>
			  <td><%= link_to enfant.prenom, enfant_path(enfant.id) %>

			  <td><%= check_box_tag(:"#{enfant.id}RepasAM", params[:"#{enfant.id}RepasAM"], @presta ? (@presta.repas !="0") : false, class: 'check_all0') %></td>

			  <td><%= check_box_tag(:"#{enfant.id}GarderieAM", params[:"#{enfant.id}GarderieAM"], @presta ? (@presta.garderieAM !="0") : false, class: 'check_all1') %></td>

			  <td><%= check_box_tag(:"#{enfant.id}GarderiePM", params[:"#{enfant.id}GarderiePM"], @presta ? (@presta.garderiePM !="0") : false, class: 'check_all2') %></td>

			  <td><%= check_box_tag(:"#{enfant.id}CentreAM", params[:"#{enfant.id}CentreAM"], @presta ? (@presta.centreAM !="0") : false, class: 'check_all3') %></td>

			  <td><%= check_box_tag(:"#{enfant.id}CentrePM", params[:"#{enfant.id}CentrePM"], @presta ? (@presta.centrePM !="0") : false, class: 'check_all4') %></td>
			</tr>
		<% end %>
	</table>
	<%= f.submit "Enregistrer" %>
<% end %>


